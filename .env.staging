# Staging. Nothing here is secret: those go in .env.staging.local.

E2E_BASE_URL=https://main.preview.openbraininstitute.org

# Other services default to this host. Name one only when it has moved.
VIRTUAL_LAB_API_URL=https://staging.cell-a.openbraininstitute.org/api/virtual-lab-manager
