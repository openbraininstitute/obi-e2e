# dependencies
node_modules/

# playwright output
test-results/
playwright-report/
blob-report/
playwright/.cache/
.e2e-runs/
.auth/

# dashboard: build output, and the fake runs `bun run site:fixtures` writes
gh-web/dist/
gh-web/fixtures/

# lighthouse output
perf/report/
.lighthouseci/

# secrets only: .env, .env.staging and .env.production are committed
.env*.local
.env.backup

# editors / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log

# ai tooling local state
.claude/settings.local.json
# agent skills written by `make install`
.claude/skills/playwright-cli/
.agents/
# playwright-cli: workspace, and snapshots/logs/profiles (may contain credentials)
.playwright/
.playwright-cli/

