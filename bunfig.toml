# `bun test` runs the unit tests next to the code they cover: the CI scripts and
# the fixture loaders. The Playwright scenarios are also `*.spec.ts`, but they
# need Playwright's own runner, so `bun run test:unit` names its roots instead of
# letting Bun walk the whole repository.
[test]
root = "scripts"
